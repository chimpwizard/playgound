apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: helloworld
  labels:
    app: helloworld
  namespace: ns-xyz
spec:
  replicas: 1
  selector:
    matchLabels:
      app: helloworld
  template:
    metadata:
      labels:
        app: helloworld
    spec:
      terminationGracePeriodSeconds: 60
      containers:
      - name: helloworld
        # Any image is permissible as long as:
        # 1. It serves a 404 page at /
        # 2. It serves 200 on a /helloworld endpoint
        image: chimpwizard/healthz
        # livenessProbe:
        #   httpGet:
        #     path: /helloworld
        #     port: 8080
        #     scheme: HTTP
        #   initialDelaySeconds: 30
        #   timeoutSeconds: 5
        ports:
        - name: http
          containerPort: 8080
        resources:
          # limits:
          #   cpu: 10m
          #   memory: 20Mi
          # requests:
          #   cpu: 10m
          #   memory: 20Mi